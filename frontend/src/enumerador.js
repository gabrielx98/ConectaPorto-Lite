const movCategoria = ['REPOSICIONAMENTO', 'EMBARQUE', 'DESCARGA', 'GATE IN', 'GATE OUT', 'PESAGEM', 'SCANNER']

const contCategoria = ['Importação', 'Exportação']

const contEstado = ['Cheio', 'Vazio']

const contTamanho = ['20 PÉS', '40 PÉS' ]

export {movCategoria, contCategoria, contEstado, contTamanho};